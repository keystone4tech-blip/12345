# PROJECT_LOG.md

## Дата: 2026-04-07
### Изменения:
- Подтверждена целостность скачанных репозиториев: `bot` и `cabinet`.
- Обнаружен новый функционал **ИИ-техподдержки** (DonMatteo-AI-Tiket).
- Переключена ветка бота на `feat-ai-support`.
- **Конфигурация и Совместимость**:
    - Применен массовый патч для совместимости с **Python 3.10** (обновлено 178 файлов). Впоследствии **успешно обновлен системный Python до 3.12** для долгосрочной стабильности.
    - Сконфигурирован `.env` с токеном бота, ключом RemnaWave и локальной БД PostgreSQL.
    - Установлен адрес панели VPN: `https://p.mozhnovpn.tech/`.
    - Успешно проведены **16 этапов миграций** базы данных (`jarvis_vpn`).
    - Инициализированы настройки провайдера **OpenRouter** через скрипт `seed_ai.py`.

### Структура проекта:
/ (рабочая директория)
  - /bot (remnawave-bedolaga-telegram-bot)
    - /app/modules/ai_ticket (Модуль ИИ-поддержки)
    - .env (Сконфигурирован под PostgreSQL и MozhnoVPN)
    - /venv (Виртуальная среда Python 3.12)
  - /cabinet (bedolaga-cabinet)
  - PROJECT_LOG.md

### Заметки:
- ИИ-помощник настроен на работу через OpenRouter (модель `qwen/qwen3.6-plus:free`).
- База данных полностью готова к работе.
- Топик форума `-1003717352634` установлен для приема тикетов.

### Важные предупреждения:
- Система переведена на **Docker** (Бот + PostgreSQL + Redis).
- Все ключи безопасности (Бот, БД, API) прописаны в `bot/.env`.

---

## 🛠 Инструкция по управлению проектом

### 1. Запуск Бота (через Docker)
Бот и его база данных работают в контейнерах. Для управления используйте терминал в папке `/bot`:

*   🚀 **Запустить**: `docker compose up -d`
*   🛑 **Остановить**: `docker compose down`
*   📜 **Посмотреть логи**: `docker compose logs -f bot`
*   🔄 **Пересобрать (при изменении кода)**: `docker compose up -d --build`

### 2. Запуск Личного Кабинета (локально)
Кабинет запускается через Node.js. Для управления используйте терминал в папке `/cabinet`:

*   📦 **Установить зависимости** (один раз): `npm install`
*   ▶️ **Запустить в режиме разработки**: `npm run dev`
*   🌐 **Открыть в браузере**: [http://localhost:3020](http://localhost:3020)

### 3. Связка компонентов
- **Бот** слушает API на порту `8080` (в Docker) или `8000` (локально).
- **Кабинет** настроен на `http://localhost:8000` для связи с базой данных через API бота.
- **Важно**: Для работы кабинета бот должен быть запущен!
