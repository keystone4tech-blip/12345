# PROJECT_LOG.md

## Дата: 2026-05-05
### Изменения:
- **Проведен полный аудит проекта**: Проверены папки `bot`, `cabinet`, настройки Docker и Nginx.
- **Создан файл [PROJECT_CONFIG_BACKUP.md](file:///c:/Users/Keystone-Tech/Desktop/%D1%81%D0%B5%D1%80%D0%B2%D0%B8%D1%81%20%D1%80%D0%B5%D0%BA%D0%BB%D0%B0%D0%BC%D1%8B%20%D1%81%20%D0%B2%D0%BF%D0%BD/PROJECT_CONFIG_BACKUP.md)**: Собраны все пароли от БД, сервера, ключи API и системные настройки для быстрого восстановления.
- Собрана информация о доменах, портах и структуре Docker-контейнеров.

### Актуальная структура:
- `/bot` — Telegram бот (Python 3.13)
- `/cabinet` — Личный кабинет (Vite + React + Nginx)
- `/nginx` — Конфигурации для Reverse Proxy
- `/scripts` — Скрипты автоматизации и деплоя
- `/mozhnovpn-deploy` — (Только для справки) Старые конфигурации панели
- `docker-compose.yml` — Основной файл оркестрации всех служб

### Заметки:
- Создан `PROJECT_CONFIG_BACKUP.md` с критическими доступами.

## Дата: 2026-05-05 (Часть 2)
### Изменения:
- **Авто-логин**: 
  - Добавлен эндпоинт `/cabinet/auth/login/auto` на бэкенд.
  - Реализована генерация короткоживущих JWT токенов (`create_autologin_token`).
  - Обновлена функция `build_cabinet_url`: теперь ссылки из бота содержат токен авторизации.
  - Исправлены ошибки в RBAC и логике сессий в `auth.py`.
- **Email/SMTP**:
  - Обновлен `.env.example` с подробными инструкциями по настройке SMTP.
  - Выделены критические параметры для работы верификации почты.

### Структура:
- `bot/app/cabinet/routes/auth.py` - новые роуты.
- `bot/app/cabinet/auth/jwt_handler.py` - генерация токенов.
- `bot/app/utils/miniapp_buttons.py` - логика ссылок.

### Заметки:
- Для активации авто-логина в реальном окружении необходимо пересобрать контейнер бота.
- Пользователю нужно заполнить SMTP данные в `.env` на сервере для работы Email.
- Файл `PROJECT_CONFIG_BACKUP.md` является "замороженным" и обновляется только по запросу.
- Текущий пароль сервера найден в скрипте `remote_deploy.py`.


### План действий:
1. Добавить Google DNS (8.8.8.8) в конфигурацию `docker-compose.yml` для всех сервисов.
2. Внедрить динамический резолвинг в Nginx через `resolver 127.0.0.11` и переменные в `proxy_pass`, чтобы предотвратить падение при недоступности апстрима.

### Текущая структура:
- `/root/mozhnovpn-deploy/docker-compose.yml` (на сервере) — требует правок.
- `/etc/nginx/conf.d/default.conf` (внутри webapp) — требует оптимизации под динамический резолвинг.

### Заметки:
- Бот работает нестабильно из-за проблем с DNS.
- Панель управления недоступна для внешних запросов и синхронизации.

## Дата: 2026-04-21
### Исправление ноды (31.13.208.149):
- **Проблема**: DNS Loop в Xray приводит к отсутствию интернета у пользователей.
- **Действие**: Одобрен план по внедрению bootstrap-IP и fallback DNS.
- **Текущая структура**: 
  - `/opt/remnawave` — основная директория проекта на сервере.

## Дата: 2026-05-02
### Изменения:
- Обновлен `bot/app/handlers/start.py` (исправлена логика обработки deep-link для подарков).
### Структура:
- `/bot/app/handlers` — здесь находится логика команд.
### Заметки:
- При переходе по ссылке вида `t.me/bot?start=gift_XXX` токен подарка сохранялся в состояние FSM, но не обновлял локальный словарь `data`. Для существующих пользователей это приводило к тому, что кнопка принятия подарка не показывалась. Проблема решена.
- Изменен файл `.github/workflows/deploy.yml` для использования пароля сервера (`SERVER_PASSWORD`) вместо SSH-ключа (`SSH_PRIVATE_KEY`), чтобы упростить деплой.

## Дата: 2026-05-02 (Продолжение)
### Изменения:
- Обновлен `bot/app/handlers/admin/pricing.py` для синхронизации цен с дефолтным тарифом в режиме `tariffs`. Теперь при изменении цены периода через админку Telegram она будет обновляться и у пользователей.
- Внесены правки в `bot/app/services/system_settings_service.py`: теперь **абсолютно все** настройки, изменяемые администратором через интерфейс бота (цены, вебхуки, реферальная программа и любые другие), будут иметь безусловный приоритет и сохраняться в базу данных, игнорируя значения, прописанные в `.env` файле. Раньше изменения многих настроек тихо блокировались.
- Обновлен `bot/app/webapi/routes/health.py`: маршрут `/health` сделан публичным (убрана проверка `require_api_token`), и добавлена поддержка метода `HEAD` (так как Docker healthcheck с `wget --spider` отправляет именно `HEAD` запрос), чтобы Docker мог успешно проверять статус контейнера без получения ошибки `401 Unauthorized` или `405 Method Not Allowed`.
- Улучшена рассылка стартовых уведомлений (`bot/app/services/startup_notification_service.py`): теперь сообщения об успешном запуске или падении бота отправляются всем администраторам напрямую в личные сообщения (взятые из `ADMIN_IDS`), в дополнение к общему каналу уведомлений.
- Упрощены уведомления о новом реферале (`bot/app/services/referral_service.py`): удален текст с обещанием бонусов за пополнение как для нового пользователя, так и для пригласившего.
- Добавлена новая настройка `SKIP_LANGUAGE_SELECTION_AT_START` (по умолчанию `True`). Теперь выбор языка при регистрации по умолчанию пропускается (язык определяется автоматически по профилю Telegram), но функция смены языка в меню продолжает работать (параметр `LANGUAGE_SELECTION_ENABLED` должен быть включен).
- Обновлен шаблон пригласительного сообщения (`bot/app/handlers/referral.py`): убрано прямое упоминание VPN и бонусов за пополнение. Исправлено форматирование при нажатии кнопки «Поделиться»: теперь сообщение отправляется без префикса с именем бота и без лишних HTML-тегов (Premium эмодзи), что делает текст чистым и готовым к пересылке.
- Обновлены файлы локализации (`bot/locales/ru.json` и `bot/app/localization/locales/ru.json`): значения ключей `REFERRAL_INVITE_*` теперь соответствуют новому варианту текста без упоминания VPN, что гарантирует использование актуального текста даже при наличии старых данных в файлах перевода.
- В класс `Texts` (`bot/app/localization/texts.py`) добавлен метод `t_raw` для получения текста без автоматической замены эмодзи на Premium-теги.
- **БЕЗОПАСНОСТЬ:** Удален скомпрометированный API токен CryptoBot из файла `bot/.env.example`.
- Обновлен интерфейс реферальной программы (`bot/app/handlers/referral.py`): теперь строки с бонусами и доходами (всего/за месяц) автоматически скрываются, если они равны нулю. Текст «Заработано всего» изменен на «Вам начислено на баланс». Заголовок «Как работают награды» изменен на «Ваши бонусы от сервиса», и теперь весь этот раздел (включая заголовок и проценты) полностью скрывается, если все значения равны нулю. Удалена статистика «Сделали первое пополнение», а призыв в конце изменен на «📢 Приглашайте друзей и получай бонусы!».

## Дата: 2026-05-03
### Изменения:
- **Реализована полная настройка Reply-кнопок главного меню через админ-панель.**
- В `bot/app/config.py` добавлено 12 новых параметров для управления текстом, цветом (стилем) и премиум-эмодзи 4-х основных кнопок (Статус, Подключиться, Оплата, Помощь).
- В админ-панели (Конфигурация бота) создан новый раздел **«⌨️ Реплай-кнопки»**, разделенный на 4 подкатегории для каждой кнопки соответственно.
- **Внедрена система выбора цвета (Choice) для стилей реплай-кнопок.** Теперь вместо ручного ввода текста в админке доступен удобный выбор из выпадающего списка (Синий, Зеленый, Красный, Серый), аналогично системе подарков.
- Обновлена логика в `bot/app/keyboards/reply.py`: теперь кнопки генерируются динамически с использованием возможностей **Telegram Bot API 9.4+** (параметры `style` и `icon_custom_emoji_id`).
- Улучшена логика фильтрации нулевых значений в реферальной программе (`bot/app/handlers/referral.py`): теперь бонусы принудительно приводятся к `int` перед проверкой, а пустые секции и нулевые доходы скрываются гарантированно.
- **Изменен порядок кнопок в главном меню (Inline):**
    - Кнопки **Инфо** и **Техподдержка** теперь расположены в одной строке в самом низу.
    - Кнопка **Поделиться с другом** (Рефералы) вынесена в отдельную строку под кнопками Подписки и Промокода.
    - Кнопка **Сделать подарок** вынесена в отдельную строку под кнопкой «Поделиться с другом».
    - Исправлена ошибка, из-за которой у пользователей с активной подпиской пропадала кнопка управления подпиской в новом макете.
    - Изменения применены как для стандартного режима меню, так и для режима «Кабинет».
- Исправлена логика парсинга ссылок подарков в `bot/app/handlers/start.py`: теперь бот сначала проверяет префикс `gift_`, а только потом ищет рекламные кампании. Это устраняет проблему, из-за которой существующие пользователи не могли открыть подарок по ссылке.
- **Исправлен процесс регистрации и уведомлений:**
    - Исправлен баг, при котором новые пользователи не видели сообщение о подарке после завершения регистрации. Логика проверки подарка перенесена в финальную стадию регистрации в `complete_registration_from_callback`.
    - Исправлена ошибка `EFFECT_ID_INVALID` в `referral_service.py`, которая приводила к падению бота и блокировке уведомлений о рефералах.
    - Добавлена автоматическая привязка к администратору для «органических» пользователей (тех, кто зашел без ссылки и пропустил ввод кода) в `complete_registration_from_callback`.
    - Реализованы специальные уведомления для администратора об органических регистрациях («👤 Органическая регистрация!»).
- **Оптимизация текстов подарков:**
    - В `bot/app/handlers/gift_vpn.py` перефразированы сообщения для подарков: убрано прямое упоминание VPN и исправлена ошибка с HTML-тегами в названии тарифа.

## Дата: 2026-05-03 (Продолжение)
### Изменения:
- **Исправлен NameError в админ-панели:** В файлах `bot/app/handlers/admin/user_messages.py` и `bot/app/handlers/admin/polls.py` добавлена отсутствующая импортация функции `strip_html`. Это приводило к падению бота при попытке просмотра списка сообщений или опросов в админке.
- **Оптимизация регистрации и подарков (Final Fix):**
    - В `bot/app/handlers/start.py` данные (`gift_token`, `referrer_id` и др.) теперь извлекаются из состояния FSM **до** вызова `state.clear()`. Это гарантирует, что подарки и реферальные привязки корректно обрабатываются в конце процесса регистрации.
    - В `bot/app/services/referral_service.py` улучшена логика уведомлений об органических регистрациях.

### Актуальная структура:
- `bot/app/handlers/start.py` — Основная логика регистрации и входа.
- `bot/app/services/referral_service.py` — Обработка реферальных связей и уведомлений.
- `bot/app/handlers/admin/user_messages.py` — Управление рекламными сообщениями в меню.
- `bot/app/handlers/admin/polls.py` — Управление опросами.

### Заметки:
- Для корректного отображения цветов (`style`) и кастомных эмодзи требуется версия Telegram клиента, поддерживающая Bot API 9.4+ (февраль 2026 и новее).
- Настройка `icon_custom_emoji_id` требует, чтобы владелец бота имел Telegram Premium для возможности использования кастомных эмодзи ботом.
- **Исправлена логика привязки рефералов:** Ранее при автоматической привязке пользователя к администратору (для органического трафика) использовался Telegram ID администратора вместо его внутреннего ID в базе данных. Это приводило к ошибкам целостности БД и прерыванию процесса регистрации. Теперь Telegram ID корректно преобразуется в Database ID.
- **Решена проблема:** При нажатии кнопки «Пропустить» без реферального кода регистрация теперь завершается успешно.

## Дата: 2026-05-03 (Продолжение)
### Изменения:
- **Проведен анализ подключения Кабинета (`/cabinet`) к Боту (`/bot`).**
- Изучены настройки `.env` в обоих проектах и конфигурация `docker-compose.yml`.
- Подтверждена совместимость кабинета с новыми изменениями в `system_settings_service.py` (приоритет БД).
- Создан implementation_plan.md с пошаговым руководством по подключению.
### Структура:
- `nginx/reverse-proxy.conf` — ключевой файл для связки фронтенда и бэкенда через единый домен.
### Заметки:
- Для корректной работы в production необходимо настроить SSL (Certbot) на домене кабинета.
- Параметр `VITE_API_URL` в кабинете должен быть `/api`, а Nginx должен проксировать этот путь на `bot:8080/cabinet/`.

## Дата: 2026-05-03 (Продолжение)
### Изменения:
- **Исправлена привязка Личного Кабинета к основному боту.**
- Юзернейм бота изменен на актуальный `MozhnoVPN_Robot`.
- Обновлен `BOT_TOKEN` в `bot/.env` (заменен тестовый токен на основной).
- Название приложения в кабинете изменено на `MozhnoVPN Cabinet`.
- Кабинет пересобран на сервере с новыми переменными окружения.
- Успешно настроен SSL-сертификат через Certbot для `lk.mozhnovpn.tech`.
### Заметки:
- Важно: токен бота в `bot/.env` теперь соответствует `@MozhnoVPN_Robot`.

## Дата: 2026-05-03 (Финал настройки Кабинета)
### Изменения:
- **Исправлена критическая ошибка 404 при запросах к API из Личного Кабинета.**
- **Nginx**: Исправлена конфигурация `proxy_pass`. Ранее префикс `/cabinet/` дублировался, что приводило к ошибкам. Теперь `/api/` корректно проксируется на корень бота (`http://127.0.0.1:8080/`).
- **API Verification**: Проверена доступность эндпоинта `/api/cabinet/branding` через HTTPS — возвращает 200 OK.
- **Nginx Cleanup**: Удалены лишние мастер-процессы Nginx на сервере, служба перезапущена через systemctl.
- **Авторизация**: Теперь Mini App может успешно отправлять `initData` на бэкенд для автоматического входа пользователей.
### Структура:
- `/etc/nginx/sites-enabled/mozhnovpn` — актуальный конфиг прокси.
- `/opt/mozhnovpn/bot/.env` — содержит `CABINET_URL=https://lk.mozhnovpn.tech`.
### Заметки:
- Проблема «белого экрана» или «бесконечной загрузки» в Telegram Mini App решена исправлением путей API.

## Дата: 2026-05-04
### Изменения:
- **Изменен формат реферальных ссылок в Личном Кабинете:**
    - В `cabinet/src/pages/Referral.tsx` ссылка теперь генерируется без префикса `ref=`, что соответствует формату ссылок бота (например, `.../login?reffHkR1wAC`).
    - В `cabinet/src/utils/referral.ts` обновлена логика захвата кода: теперь система умеет извлекать код, если он передан как ключ в строке запроса (`?reff...`), сохраняя при этом поддержку старого формата (`?ref=...`).
    - Добавлено логирование процесса захвата кода в консоль браузера для облегчения отладки.
- **Исправлена проблема с применением настроек в Кабинете:**
    - В `bot/app/services/system_settings_service.py` исправлен метод `set_value`. Теперь любые изменения настроек, внесенные через интерфейс, применяются немедленно, даже если эти настройки были изначально прописаны в файле `.env`. Ранее такие изменения «зависали» до перезагрузки бота.
    - Проверен список `READ_ONLY_KEYS` — он ограничен только системными токенами, что гарантирует доступность большинства настроек для редактирования.
- **GitHub**: Репозиторий обновлен, все последние изменения закоммичены.
- **Улучшена система CI/CD (Автодеплой):**
    - В `deploy.yml` расширен список отслеживаемых путей (теперь учитываются изменения в `nginx/`, `.env.example` и корневых конфигах).
    - Внедрена система «пробития кэша» через `BUILD_TIMESTAMP`. Теперь при каждом деплое генерируется уникальная временная метка, которая передается в Docker как `ARG`. Это гарантирует, что Docker не пропустит сборку фронтенда (кабинета), даже если его кэш кажется актуальным.
    - Обновлен `scripts/deploy.sh`: теперь перед сборкой в логи выводится список реально изменившихся файлов (`git diff`), что помогает отлаживать процесс обновления.
    - В `docker-compose.yml` и оба `Dockerfile` (бот и кабинет) добавлена поддержка аргумента `BUILD_TIMESTAMP`.
    - Список выполненных задач:
        - [x] Оптимизировать `deploy.yml` (триггеры и переменные)
        - [x] Добавить `BUILD_TIMESTAMP` в `docker-compose.yml`
        - [x] Улучшить `scripts/deploy.sh` (логика пересборки и логирование)
        - [x] Обновить `PROJECT_LOG.md`
        - [x] Проверить работу системы

### Актуальная структура:
- `.github/workflows/deploy.yml` — Конфигурация GitHub Actions.
- `scripts/deploy.sh` — Скрипт развертывания на сервере.
- `docker-compose.yml` — Описание сервисов и аргументов сборки.

### Заметки:
- Теперь изменения в папке `cabinet/` будут подхватываться так же надежно, как и в `bot/`.
- Если изменения не видны, проверьте вкладку **Actions** в GitHub на наличие ошибок в логах сборки.

## Дата: 2026-05-04 (Откат к стабильной версии)
### Изменения:
- **Произведен полный откат репозитория к коммиту `145b636` ("chore: improve CI/CD pipeline...").**
- Откат выполнен локально (`git reset --hard`) и на GitHub (`git push --force`).
- Запущен автоматический деплой на сервер через GitHub Actions.
- Это действие отменило изменения, связанные с `EmailService` (SSL/TLS) и фиксами Nginx, внесенные в коммитах `190de58`, `1ccd4ff` и `ce5e0e6`.
### Заметки:
- Бот и кабинет находятся в процессе пересборки на сервере.
- Причина отката: восстановление работоспособного состояния по запросу пользователя.
- Требуется дождаться завершения Workflow в GitHub и проверить логи бота.

## Дата: 2026-05-04 (Восстановление старой конфигурации Docker)
### Изменения:
- **Откат Docker-конфигурации:** Файлы `bot/Dockerfile`, `cabinet/Dockerfile`, `docker-compose.yml` и `scripts/deploy.sh` восстановлены до состояния коммита `1bf2e39`.
- Удалена экспериментальная система пробития кэша через `BUILD_TIMESTAMP`, которая вызывала ошибки на сервере.
- Возвращена стандартная схема сборки без принудительного `--no-cache` в скрипте деплоя (по умолчанию).
### Заметки:
- Цель: Вернуть проект в гарантированно рабочее состояние, которое было до сегодняшних правок в пайплайне деплоя.
- Требуется проверка логов на сервере после завершения GitHub Actions.


## Дата: 2026-05-05 (Запуск сервисов и диагностика)
### Изменения:
- **Запуск Nginx на хосте:** Обнаружено, что `nginx.service` на сервере был отключен и неактивен. Выполнен запуск (`systemctl start nginx`), что восстановило доступ к кабинету по домену `lk.mozhnovpn.tech`.
- Исправлен статус 'unhealthy' кабинета (замена wget на curl в Dockerfile).
- Восстановлена работа Nginx на сервере.

## Дата: 2026-05-05 (Сессия 2)
### Изменения:
- Обновлен файл `PROJECT_CONFIG_BACKUP.md` актуальными данными (GitHub, JWT, SMTP notes).
- Переведен кабинет на Debian-based образы для решения проблем с кэшем Docker.
- Начата работа по мобильной адаптации административной панели кабинета.

### Текущий статус:
- Инфраструктура стабильна.
- В процессе: улучшение UX на мобильных устройствах.
- **Глубокая диагностика:** Проведена проверка всех компонентов. Бот (`remnawave_bot`) работает исправно, база данных и redis доступны.
### Структура:
- Без изменений.
### Заметки:
- Кабинет теперь доступен.
- Важно: необходимо следить за состоянием Nginx на хосте. В будущем рекомендуется перевести прокси в Docker-контейнер для большей автономности.
- Исправлен NameError в `admin/user_messages.py` (импортирован `strip_html`).

## Дата: 2026-05-05 (Сессия 3 - Мобильная адаптация)
### Изменения:
- **Завершена мобильная адаптация админ-панели**:
  - `BrandingTab.tsx`: Логотип и название теперь перестраиваются в колонку на узких экранах, кнопки управления адаптированы.
  - `ThemeTab.tsx`: Сетка пресетов переведена в 1 колонку, кнопки «Сохранить/Отмена» теперь занимают всю ширину на мобильных.
  - `globals.css`: 
    - Добавлена горизонтальная прокрутка для таблиц с кастомным скроллбаром.
    - Уменьшены отступы карточек на мобильных (с 1.25rem до 1rem) для экономии пространства.
    - Добавлен класс `.cell-wrap` для корректного отображения длинного контента.
  - `AdminBanSystem.tsx`: Все контейнеры таблиц переведены с `overflow-hidden` на `overflow-x-auto`.
  - `SettingInput.tsx`: Поля ввода теперь имеют адаптивную ширину (`w-full max-w-[12rem]`), что предотвращает горизонтальный скролл страницы.

### Актуальная структура:
- `/cabinet/src/components/admin` — адаптированные компоненты.
- `/cabinet/src/styles/globals.css` — обновленные глобальные стили.

### Заметки:
- Все таблицы в админке теперь корректно скроллируются по горизонтали на смартфонах.
- Интерфейс выглядит премиально и удобен для управления через Telegram Mini App.
- **ВАЖНО**: При добавлении новых таблиц всегда оборачивайте их в `div` с классом `overflow-x-auto` или `table-container`.
