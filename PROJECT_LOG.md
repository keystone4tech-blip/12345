# Журнал проекта (PROJECT_LOG.md)

## Дата: 2026-04-09
### Изменения:
- **Администрирование: Полное удаление данных (Wipe User)**:
    - Реализован функционал «Стереть пользователя полностью», позволяющий безвозвратно удалять все записи о пользователе из БД (транзакции, история, профиль) и принудительно удалять его аккаунт из Remnawave.
    - [x] Реализована функция "☢️ Стереть пользователя" (Wipe User) в админ-панели (удаление из БД и RemnaWave панели).
- [x] Исправлены и обновлены визуальные эффекты (message_effect_id) для Telegram Premium:
    - ❤️ Сердце при отказе от правил и получении подарка.
    - 🔥 Огонь при завершении регистрации.
    - 🎉 Праздник при покупке тарифа, регистрации и уведомлениях о рефералах.
    - 👏 Аплодисменты при запросе реферального кода и уведомлении реферера о новом пользователе.
    - Исправлена критическая ошибка: эффекты не отображались при активации пробного периода (использовался `edit_text`).
    - Оптимизирован UX: теперь старые сообщения удаляются перед отправкой новых с эффектами, что избавляет от наслоения и гарантирует фокус на анимации.
    - Обновлены файлы: `purchase.py`, `tariff_purchase.py`, `promo.py`, `start.py`.
- **Конфигурация: Анализ и фиксация Premium-эмодзи**:
    - Успешно извлечена карта соответствия Premium-эмодзи напрямую из базы данных (ключ `PREMIUM_EMOJIS_DATA`).
    - **Хардкод дефолтных настроек**: Все 115 привязок эмодзи прописаны как значения по умолчанию в `bot/app/config.py`.
    - Активирован флаг `USE_PREMIUM_EMOJIS=True` по умолчанию.
    - Теперь при развертывании на новую БД бот автоматически будет использовать настроенные Premium-эмодзи без необходимости ручного ввода.
    - Обновлен файл: `bot/app/config.py`.
- **Динамическое управление Premium-эмодзи**:
    - Добавлена возможность добавлять новые стандартные эмодзи в список для настройки прямо через бота.
    - В админ-панели (`Интерфейс и брендинг -> Настройка Premium эмодзи`) добавлена кнопка `➕ Добавить новый эмодзи`.
    - Реализована динамическая пересборка регулярного выражения для замены эмодзи в сообщениях при изменении списка в базе данных.
    - Теперь новые добавленные символы сразу учитываются системой и доступны для маппинга.
    - Обновлены файлы: `app/utils/premium_emojis.py`, `app/handlers/admin/bot_configuration.py`, `app/states.py`.

## Дата: 2026-04-09
### Изменения:
- **Улучшение визуального опыта (🎉 Premium Effects)**:
        - Уведомление пригласителя о новом реферале (👏 **Applause** в `referral_service.py`).
        - Успешная активация подписки в простом режиме (`simple_subscription.py`).
        - Активация тестового доступа и скидок через промо-предложения (`promo.py`).
        - Покупка суточного тарифа и продление подписки (`tariff_purchase.py`).
        - Стандартная активация триала (`purchase.py`).
        - Успешная активация промокода (`promocode.py`).
    - Функция `send_referral_notification` в `referral_service.py` теперь поддерживает передачу `message_effect_id`.


## Дата: 2026-04-09
### Изменения:
- **Улучшение вовлеченности через подарки (Gift-Referral Linking)**:
    - Реализована автоматическая привязка реферера при использовании ссылки на подарок. Теперь пользователь, подаривший подписку, автоматически становится реферером получателя.
    - Изменения внесены в `bot/app/handlers/start.py` (логика обработки `gift_` токена в `/start`).
- **Автоопределение языка (UX)**:
    - Бот теперь автоматически определяет язык пользователя на основе настроек его профиля в Telegram (ru/en/fa).
    - Если язык доступен в системе, он устанавливается по умолчанию, минуя лишние диалоги выбора языка (если это позволяют общие настройки).
- **Визуальные эффекты Telegram (Premium UX)**:
    - Внедрены `message_effect_id` для ключевых взаимодействий:
        - 🎉 **Праздник** при показе правил.
        - 👏 **Аплодисменты** при успешном завершении регистрации и входе в меню.
        - ❤️ **Сердце** при получении предложения подарка (как для новых, так и для существующих пользователей).
        - 👍 **Лайк** при уведомлении дарителя об успешной активации его подарка.
    - Обновлены файлы: `bot/app/handlers/start.py`, `bot/app/services/gift_service.py`.


## Дата: 2026-04-09
### Изменения:
- **Унификация динамических кнопок**:
    - Все кнопки «Купить подписку» теперь используют настройки текста, цвета и премиум-эмодзи из админ-панели (включая разделы управления, промо-акции и уведомления об оплате).
    - Добавлены новые настройки `SUBSCRIPTION_BUTTON_STYLE` и `SUBSCRIPTION_BUTTON_EMOJI` для полной кастомизации основной кнопки управления подпиской.
    *   Обновлены файлы: `config.py`, `system_settings_service.py`, `inline.py`, `promo.py`, `common.py`.
- **Брендирование пользователей в RemnaWave**:
    - Изменен стандартный шаблон имени пользователя с `user_{telegram_id}` на `MozhnoVPN_{telegram_id}`.
    - Обновлены конфигурационные файлы `config.py` и `system_settings_service.py`.
- **Глобальная гуманизация настроек (Этап 1: Цены и Тарифы)**:
    - Обновлен словарь `SETTING_HINTS` в `bot/app/services/system_settings_service.py`.
    - Добавлены подробные описания для тарифов, лимитов и платежных систем.
- **Повышение стабильности сетевых запросов**:
    - Внедрен механизм повторов и таймаутов для `disposable_email_service.py` и `blacklist_service.py`.
- **Интеграция Cabinet Mini App**:
    - Настройка CORS (`CABINET_ALLOWED_ORIGINS`) и окружения для локального тестирования.
- **Динамическая кастомизация кнопок**:
    - Добавлены настройки для кнопок «Партнерка» и «Купить подписку» (текст, стиль, эмодзи).
    - Обновлены клавиатуры в `inline.py` и `reply.py`.
- **Ребрендинг в MozhnoVPN**:
    - Все упоминания "Remnawave Bedolaga Bot" заменены на "MozhnoVPN Bot".
    - Обновлены локализации (ru.yml, en.yml, texts.py) для приветствий и поддержки.
    - Обновлен баннер запуска в `main.py` и уведомления в `startup_notification_service.py`.
    - Изменены метаданные в `Dockerfile` и `pyproject.toml`.
    - **Глубокая чистка**: Удалены ссылки на Telegram-каналы и контакты прошлых разработчиков (`fringg`, `BEDOLAGA-DEV`). Все системные уведомления теперь ведут на вашу поддержку.
- **Миграция репозитория обновлений**:
    - Основной репозиторий для проверки новых версий изменен с `fr1ngg` на `keystone4tech-blip/12345`.
    - Обновлены конфигурации в `config.py`, `version_service.py` и `admin_notification_service.py`.
    - Обновлены метаданные в `Dockerfile` (ссылки на source и url).
- **Унификация кнопки «Подключиться»**:
    - Реализована централизованная функция `get_dynamic_connect_button` для генерации кнопок подключения по всему боту.
    - Добавлены настройки в админ-панель: `CONNECT_BUTTON_TEXT`, `CONNECT_BUTTON_STYLE` и `CONNECT_BUTTON_EMOJI`.
    - Все разделы бота (Главное меню, Моя подписка, Гиды по настройке, Уведомления и Подарки) теперь используют единый стиль кнопки подключения.
    - Автоматическая очистка стандартных эмодзи при использовании премиум-эмодзи (защита от дублирования).
    - Обновлены файлы: `config.py`, `system_settings_service.py`, `inline.py`, `purchase.py`, `links.py`, `gift_vpn.py`, `remnawave_webhook_service.py`.
- **Исправление SyntaxError**:
    - Устранена ошибка в `system_settings_service.py`, проведена дедупликация настроек.

### Статус:
- Проект успешно проходит проверку синтаксиса.
- Все изменения синхронизированы с GitHub.

## Дата: 2026-04-08 (Исправление критической ошибки запуска)
### Изменения:
- **Hotfix (TelegramBadRequest)**:
  - `bot/app/utils/premium_emojis.py`: Усилена валидация в Middleware.
- **Hotfix (UnboundLocalError)**:
  - `bot/app/handlers/start.py`: Исправлена ошибка в `/start`.
- **UX/UI Improvements**:
  - Реализован бесшовный возврат к сетке эмодзи.
  - Добавлена кнопка «Изменить» для привязок эмодзи.
- **Rich Text & Premium Emoji Migration**:
  - Полная поддержка HTML в тарифах, серверах и рассылках.

### Заметки:
- Теперь интерфейс администратора полностью свободен от сырых названий переменных.
- Система подарков полностью функциональна.
